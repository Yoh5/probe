# Copy to .env and fill in. Never commit .env.
# Create the key at https://www.assemblyai.com/dashboard. It stays on the server:
# the browser only receives single-use temporary tokens.
ASSEMBLYAI_API_KEY=
